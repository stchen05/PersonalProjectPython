[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "personal-project"
version = "0.1.0"
description = "A small Python project scaffold."
readme = "README.md"
requires-python = ">=3.8"
authors = [ { name = "You" } ]
